# Documentation: http://docs.travis-ci.com/user/languages/julia/
language: julia
os:
  - linux
  - osx
julia:
  - 1.1
  - nightly
notifications:
  email: false
# uncomment the following lines to override the default test script
#script:
#  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
#  - julia -e 'Pkg.clone(pwd()); Pkg.build("polyline"); Pkg.test("polyline"; coverage=true)'
after_success:
  - julia -e 'using Pkg; Pkg.add("Coverage"); Pkg.dir("Polyline"); using Coverage; Coveralls.submit(Coveralls.process_folder())'
#  - julia -e 'using Pkg; ps=Pkg.PackageSpec(name="Documenter", version="0.19"); Pkg.add(ps); Pkg.pin(ps); Pkg.dir("polyline"); include(joinpath("docs", "make.jl"))'
